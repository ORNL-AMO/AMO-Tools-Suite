#language: cpp
language: node_js
node_js:
  - '12.8.0'

os:
  #- linux
  #- osx
  - windows

compiler:
  - gcc
#  - clang

before_install:
  #- choco install -y nodejs.install --version 12.8.0
  #- choco install -y --force nodejs@v12.8.0
  #- npm i -g npm
  - choco install -y make
  - node --version
  - choco install -y doxygen.install
  #- npm --version

install: travis_retry npm install

script:
  - ls -a
  - node --version
  - cmake ./
  - ls -a
  #- make
  #- ./bin/amo_tools_suite_tests
  #- npm install
  #- npm run test
  #- npm run at
  #- doxygen Doxyfile.in
