language: cpp


os:
  - linux
  - osx

compiler:
  - gcc
#  - clang

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then
      brew update;
      brew install npm;

    else
      sudo apt-get update;
      sudo apt-get install npm;
    fi

install: npm install

script:
  - cmake ./
  - make
  - ./bin/amo_tools_suite_tests
  - npm run test
